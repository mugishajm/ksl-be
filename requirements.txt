# --- Web API + camera sign detection (api_server.py, keypoint_asl_engine.py) ---
# Use Python 3.11 or 3.12 for reliable pre-built wheels (TensorFlow, MediaPipe, OpenCV).
# Avoid 3.13+ unless you know wheels exist for your platform.

Flask>=3.0.3,<4
flask-cors>=4.0.1
numpy>=1.26.4,<2.1
pandas>=2.0.0
scikit-learn>=1.4.0
scipy>=1.11.0
joblib>=1.3.0
opencv-python>=4.8.0,<5
mediapipe==0.10.14
Pillow>=10.0.0
protobuf>=4.25.3,<5
requests>=2.31.0

# Keypoint ASL (supportbackend .tflite). Comment out if install fails; API falls back to sklearn.
tensorflow>=2.15.0,<2.18.0
