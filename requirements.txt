# --- Web API + camera sign detection (api_server.py, keypoint_asl_engine.py) ---
# Use Python 3.11 or 3.12 for reliable pre-built wheels (TensorFlow, MediaPipe, OpenCV).
# Avoid 3.13+ unless you know wheels exist for your platform.
# This project is not supported on Python 3.14+ on Windows because core deps (NumPy/Pandas)
# may require a native compiler toolchain and will fail to install.

Flask>=3.0.3,<4
flask-cors>=4.0.1
# NumPy + OpenCV baseline (supported on Python 3.11–3.13).
numpy>=1.26.4,<2.1; python_version < "3.14"
pandas>=2.0.0
scikit-learn>=1.4.0
scipy>=1.11.0
joblib>=1.3.0
opencv-python>=4.8.0,<5
# mediapipe 0.10.14 has no Python 3.13+ wheels; use newer builds there.
mediapipe==0.10.14; python_version < "3.13"
mediapipe>=0.10.30; python_version >= "3.13"
Pillow>=10.0.0
protobuf>=4.25.3,<5
requests>=2.31.0
pymongo>=4.8.0

# Keypoint ASL (.tflite) support. TensorFlow wheels are generally unavailable on Python 3.13+.
# On newer Python, API still works via sklearn fallback if TF isn't installed.
tensorflow>=2.15.0,<2.18.0; python_version < "3.13"
